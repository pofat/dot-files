# ~/.tmuxinator/xcode.yml

name: Xcode
root: ~/Develop/Apple/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
on_project_start: mkdir -p /tmp/mudox/log/Xcode/Jack &>/dev/null

# Runs in each window and pane before window/pane specific commands. Useful for
# setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different
# tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers
# like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project
# startup. If not set, the first window is used.
# startup_window: logs

# Controls whether the tmux session should be attached to automatically.
# Defaults to true.
attach: false

# Runs after everything. Use it to attach to tmux with custom options etc.
# post: tmux -CC attach -t xcode

windows:

   - Log:
      layout: 936b,211x52,0,0{150x52,0,0,9,60x52,151,0,51}
      panes:
         - cd /tmp/mudox/log/Xcode/Jack
         - cd /tmp/mudox/log/Xcode

   - Jack:
      - cd ~/Develop/apple/Frameworks/Jack
      - vv -S

   - MudoxKit:
      - cd ~/Develop/apple/Frameworks/MudoxKit
      - vv -S

   - SocialKit:
      - cd ~/Develop/apple/Frameworks/SocialShare
      - vv -S
