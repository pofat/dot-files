# ~/.tmuxinator/mudox.yml

name: Mudox
root: ~/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for
# setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different
# tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers
# like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project
# startup. If not set, the first window is used.
# startup_window: logs

# Controls whether the tmux session should be attached to automatically.
# Defaults to true.
attach: false

# Runs after everything. Use it to attach to tmux with custom options etc.
# post: tmux send -t 'Mudox:Dashboard.2' 'tmux clock-mode' c-m

windows:

   - Console: # Run arbitrary command here

   - Dashboard:
      layout: ae93,211x52,0,0{135x52,0,0,2,75x52,136,0[75x17,136,0,3,75x34,136,18,4]}
      panes:
         - htop
         - date # to be filled show other info
         - ~/Git/dot-files/scripts/cmdhub/osx/test_terminal_color_capability

   - Network: sudo /usr/local/Cellar/iftop/1.0pre4/sbin/iftop

   - Tmuxinator:
      - cd ~/.tmuxinator
      - vv -S
