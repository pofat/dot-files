export PYENV_ROOT="${MDX_REPOS_ROOT}/pyenv"

mdx_path_push_front "${PYENV_ROOT}/bin"

eval "$(pyenv init -)"

# completion support in interactive mode of zsh.
if [[ $- != *i* ]]; then
  fpath+="${MDX_REPOS_ROOT}/pyenv/completions/pyenv.zsh"
fi

# vim:filetype=zsh
